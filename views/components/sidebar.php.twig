<aside id="sidebar" class="bg-white shadow-xl border-r border-blue-100 p-6 w-64 min-h-screen rounded-r-2xl flex flex-col gap-8">
    <div class="flex flex-col items-center gap-2 mb-2">
        {% if restaurantName is defined %}
            <div class="w-20 h-20 mb-1 flex items-center justify-center rounded-full overflow-hidden">
                <img src="/logo" alt="Logo de {{restaurantName}}" class="w-full h-full object-contain" onerror="this.style.display='none';this.parentNode.innerHTML='<i data-feather=\'coffee\' class=\'w-14 h-14 text-blue-400\'></i>'">
            </div>
            <span class="text-xl font-extrabold text-blue-700 tracking-tight text-center">{{restaurantName}}</span>
        {% else %}
            <div class="w-20 h-20 mb-1 flex items-center justify-center rounded-full">
                <i data-feather="coffee" class="w-14 h-14 text-blue-400"></i>
            </div>
            <span class="text-xl font-extrabold text-blue-700 tracking-tight text-center">Restaurante</span>
        {% endif %}
    </div>
    <nav class="flex-1">
        <ul class="space-y-2">
            <li>
                <a href="/admin/" class="flex items-center gap-3 px-3 py-2 rounded-lg text-blue-700 hover:bg-blue-50 font-semibold transition">
                    <i data-feather="home"></i> Inicio
                </a>
            </li>
            <li>
                <a href="/admin/tables" class="flex items-center gap-3 px-3 py-2 rounded-lg text-blue-700 hover:bg-blue-50 font-semibold transition">
                    <i data-feather="grid"></i> Mesas
                </a>
            </li>
            <li>
                <a href="/admin/categories" class="flex items-center gap-3 px-3 py-2 rounded-lg text-blue-700 hover:bg-blue-50 font-semibold transition">
                    <i data-feather="tag"></i> Categorías
                </a>
            </li>
            <li>
                <a href="/admin/products" class="flex items-center gap-3 px-3 py-2 rounded-lg text-blue-700 hover:bg-blue-50 font-semibold transition">
                    <i data-feather="box"></i> Productos
                </a>
            </li>
            <li>
                <a href="/admin/composed" class="flex items-center gap-3 px-3 py-2 rounded-lg text-blue-700 hover:bg-blue-50 font-semibold transition">
                    <i data-feather="layers"></i> Productos compuestos
                </a>
            </li>
            {% if promosEnabled %}
            <li>
                <a href="/admin/promotional" class="flex items-center gap-3 px-3 py-2 rounded-lg text-blue-700 hover:bg-blue-50 font-semibold transition">
                    <i data-feather="star"></i> Productos promocionales
                </a>
            </li>
            {% endif %}
            <li>
                <a href="/admin/users" class="flex items-center gap-3 px-3 py-2 rounded-lg text-blue-700 hover:bg-blue-50 font-semibold transition">
                    <i data-feather="users"></i> Usuarios
                </a>
            </li>
            <li>
                <a href="/admin/config" class="flex items-center gap-3 px-3 py-2 rounded-lg text-blue-700 hover:bg-blue-50 font-semibold transition">
                    <i data-feather="settings"></i> Configuración
                </a>
            </li>
        </ul>
    </nav>
    <div class="mt-8">
        <a href="/" class="flex items-center gap-3 px-3 py-2 rounded-lg text-red-600 hover:bg-red-50 font-semibold transition">
            <i data-feather="log-out"></i> Salir
        </a>
    </div>
</aside>
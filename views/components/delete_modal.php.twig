{# views/components/delete_modal.php.twig #}
<dialog id="deleteConfirmModal" class="rounded-lg shadow-lg max-w-sm w-full transition-opacity duration-300">
    <form method="dialog" class="relative bg-white p-6 modal-content">
        <button id="closeDeleteModalBtn" class="absolute top-2 right-2 text-gray-400 hover:text-gray-700" type="button">&times;</button>
        <h3 class="text-xl font-bold text-center mb-2">Confirmar eliminación</h3>
        <div class="text-gray-700 text-center mb-4" id="deleteModalMessage">
            ¿Estás seguro de que deseas eliminar este elemento?
        </div>
        <div class="flex justify-center gap-4" id="deleteModalButtons">
            <button type="button" id="cancelDeleteBtn" class="btn secondary">Cancelar</button>
            <button type="button" id="confirmDeleteBtn" class="btn danger">Eliminar</button>
        </div>
        <div class="flex justify-center gap-4" id="deleteModalOnlyAdmin" style="display:none">
            <button type="button" id="onlyAdminOkBtn" class="btn primary">Vale</button>
        </div>
    </form>
    <style>
    dialog[open] {
        display: flex !important;
        align-items: center;
        justify-content: center;
        background: rgba(0,0,0,0.5);
        position: fixed;
        inset: 0;
        z-index: 50;
        padding: 0;
        width: 100vw;
        height: 100vh;
        max-width: 100vw;
        max-height: 100vh;
        margin: 0;
    }
    .modal-content {
        margin: auto;
        max-width: 400px;
        width: 100%;
        border-radius: 0.75rem;
        box-shadow: 0 10px 25px rgba(0,0,0,0.15);
        background: #fff;
        display: flex;
        flex-direction: column;
        align-items: center;
    }
    </style>
</dialog>

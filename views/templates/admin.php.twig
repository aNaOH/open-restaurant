<!DOCTYPE html>
<html>
    <head>
        {% block head %}
            <link rel="preconnect" href="https://fonts.googleapis.com">
            <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
            <link href="https://fonts.googleapis.com/css2?family=Gabarito:wght@400..900&family=Lexend:wght@100..900&display=swap" rel="stylesheet">
            <link rel="stylesheet" href="/assets/style.css" >
            <title>{% block title %}{% endblock %} - OpenRestaurant</title>
        {% endblock %}
    </head>
    <body class="bg-[#f0f0f0] w-full min-h-screen flex">
        {{ include('/components/sidebar.php.twig') }}
        <div class="flex-1 flex flex-col min-h-screen">
            <div id="content" class="flex-1 p-6">
                <div class="bg-white shadow-xl p-8 rounded-2xl w-full h-full border border-blue-100">
                    <div class="mb-6 border-b-4 border-blue-500 pb-2 flex items-center gap-3">
                        <i data-feather="settings" class="text-blue-500"></i>
                        <h1 class="text-2xl font-extrabold text-blue-700">{% block name %}{% endblock %}</h1>
                    </div>
                    {% block content %}{% endblock %}
                </div>
            </div>
            <footer class="p-4 text-center">
                <p class="text-sm text-gray-500">Web potenciada por <span class="font-bold text-blue-600">OpenRestaurant</span></p>
            </footer>
        </div>
        <script src="/assets/js/jquery-3.7.1.min.js"></script>
        <script src="/assets/js/feather.min.js"></script>
        <script>
            document.addEventListener('DOMContentLoaded', function() {
                if (window.feather) {
                    window.feather.replace();
                }
            });
        </script>
        {% block scripts %}{% endblock %}
    </body>
</html>
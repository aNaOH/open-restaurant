<!DOCTYPE html>
<html>
    <head>
        {% block head %}
            <link rel="stylesheet" href="/assets/style.css" >
            <title>{% block title %}{% endblock %} - OpenRestaurant</title>
        {% endblock %}
    </head>
    <body class="bg-[#f0f0f0] w-full h-screen flex">
        {{ include('/components/sidebar.php.twig') }}
        <div class="flex-1 flex flex-col">
            <div id="content" class="container flex-1 p-4">
                <div class="bg-white shadow-lg p-4 rounded-lg w-full h-full">
                    <div class="mb-4 border-b-2 border-b-black">
                        <h1 class="text-2xl font-bold mb-4">{% block name %}{% endblock %}</h1>
                    </div>
                    {% block content %}{% endblock %}
                </div>
            </div>
            <footer class="p-2 mb-2 text-center">
                <p class="text-sm text-gray-600">Web potenciada por OpenRestaurant</p>
            </footer>
        </div>
    
        <!-- Scripts -->
        {% block scripts %}{% endblock %}
    </body>
</html>